# Release Notes — `2.17.0-r4-f14`

**Codename:** R4-F14 Remove legacy payment routers

## Destaques

- Routers HTTP legado `pagamentos` e `payments` removidos do código.
- `LegacyGoneMiddleware` continua respondendo **410** em `/pagamentos*` e `/payments*` → `/v1/payments`.

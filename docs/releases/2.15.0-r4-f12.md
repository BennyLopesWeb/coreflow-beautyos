# Release Notes — CoreFlow BeautyOS `2.15.0-r4-f12`

**Data:** 2026-07-22
**Codename:** R4-F12 Transfer Payment no reschedule

## Destaques

- No `POST /v1/bookings/{id}/reschedule`, além de copiar `deposit_paid`,
  as linhas `payments` e `core_payments` passam a apontar para o booking
  novo (`booking_id` reatribuído).

# Release Notes — `2.16.0-r4-f13`

**Codename:** R4-F13 Booking lifecycle + frontend core paths

## Destaques

- `POST /v1/bookings/{id}/complete` e `/no-show`
- Expiração pending → `expired` (evento `booking.expired`)
- Fila conclui via state machine canônica
- Frontend deixa de chamar paths 410 nos fluxos principais (pagamentos admin, listagem, payments reservation)

# Release Notes — CoreFlow BeautyOS `2.0.0-beta.1`

**Data:** 2026-07-16  
**Codename:** R2 Core Domain Consolidation (beta)  
**Branch:** `r2-f3-resource-engine`

## Destaques

- Booking domain hexagonal (create/approve/reject/cancel) com dual-write e outbox
- Resource Engine v1 (`resource.engine.enabled`)
- Catalog/Customer repositories (ADR-030)
- Plugin Engine tipado + BeautyAgent em `app/plugins/beauty` + P10
- Hardening: fitness CI ERROR, OTEL `booking.create.core`, reconciliation drift
- **Enforcement narrow:** block escritas booking legado em staging (ADR-033)

## Feature flags (defaults)

| Flag | Default |
|------|---------|
| `FEATURE_BOOKING_CORE_ENABLED` | false |
| `FEATURE_RESOURCE_ENGINE_ENABLED` | false |
| `FEATURE_PLUGIN_ENGINE_ENABLED` | false |
| `CORE_ENFORCEMENT_MODE` | off (local) / **block** (staging) |

## Breaking (staging)

Clientes que fazem `POST/PUT/DELETE` em `/agenda/agendamentos` ou `/reservations` recebem **409** com link para `/v1/bookings`.

Ver [migration guide](../migrations/legacy-booking-to-v1.md).

## Rollback

```bash
CORE_ENFORCEMENT_MODE=warn
```

## Paridade

12/12 cenários cobertos por testes R2 (P01–P12).

## Próximos passos (R3)

- ~~Block payments/fila~~ → [2.1.0-r3-f1](./2.1.0-r3-f1.md)
- ~~Production enforcement~~ → R3-F1
- Remoção gradual de routers legado (R3-F2+)

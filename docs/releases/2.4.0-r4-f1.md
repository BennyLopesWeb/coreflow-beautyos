# Release Notes — CoreFlow BeautyOS `2.4.0-r4-f1`

**Data:** 2026-07-20  
**Codename:** R4-F1 Legacy Booking 410 Gone  
**Branch:** `r4-f1-legacy-booking-410`

## Destaques

- Rotas legado de booking (`/agenda/agendamentos`, `/agendamentos`, `/reservations`) respondem **410 Gone**
- EventCatalog: `reservation.created` → status `gone` (ADR-027)
- `/agenda/disponibilidade` permanece disponível com headers Sunset

## Breaking

Qualquer cliente que ainda leia ou escreva em `/reservations` ou `/agenda/agendamentos` recebe **410**. Migrar para `/v1/bookings`.

## Próximos passos (R4-F2)

- Remover dual-write `project_*` / sync outbound booking
- Remover model `agendamentos` (quando dual-write zero)

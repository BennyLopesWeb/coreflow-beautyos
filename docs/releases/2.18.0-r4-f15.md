# Release Notes — `2.18.0-r4-f15`

**Codename:** R4-F15 Comprovante core-only

## Destaques

- Upload de comprovante de depósito via `POST /v1/bookings/{booking_id}/comprovante`.
- Persistência em `payments` (DEPOSIT) com `booking_id` + `comprovante_url` — sem confirmar o sinal.
- Frontend restaura o envio após criar a reserva (`agendar/[id]`).
- Path legado `/pagamentos/comprovante*` continua **410 Gone**.

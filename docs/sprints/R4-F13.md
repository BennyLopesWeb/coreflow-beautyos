# Release 4 — Sprint R4-F13

**Lifecycle ADR-026 + frontend off 410** (`2.16.0-r4-f13`).

| Campo | Valor |
|-------|-------|
| **Versão** | `2.16.0-r4-f13` |
| **Status** | ✅ Implemented (tech) |
| **Anterior** | R4-F12 ✅ |

## Escopo IN

| # | Entrega |
|---|---------|
| 1 | `COMPLETED` / `NO_SHOW` / `EXPIRED` no lifecycle + ORM |
| 2 | Handlers + `POST /v1/bookings/{id}/complete\|no-show` |
| 3 | Fila `concluir` → `CompleteBookingHandler` |
| 4 | `DisponibilidadeService` expira via `ExpireBookingHandler` |
| 5 | Frontend: admin confirmar sinal booking; listar/obter v1; payments → admin booking; reservation complete/cancel/approve v1 |

## Escopo OUT

- Remoção física dos routers `pagamentos.py` / `payments.py`
- Upload de comprovante core-only (novo endpoint)
- CI AWS OIDC

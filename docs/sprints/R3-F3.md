# Release 3 — Sprint R3-F3

**Remoção de routers write legado + fila → booking core** (`2.3.0-r3-f3`).

| Campo | Valor |
|-------|-------|
| **Versão** | `2.3.0-r3-f3` |
| **Status** | ✅ Implemented (tech) |
| **Anterior** | R3-F2 ✅ |
| **RFC** | RFC-003 M5 |

## Escopo IN

| # | Entrega |
|---|---------|
| 1 | Remover POST/PUT/DELETE de `/agenda/agendamentos` (manter GET + disponibilidade) |
| 2 | Remover writes de `/reservations` (manter GET) |
| 3 | Migrar `QueueEntryService.aprovar_com_horario` → `CreateBookingHandler` |
| 4 | `entrar()` seta `company_id` (default 1) |
| 5 | Testes R3-F3 + release notes |

## Escopo OUT

| Item | Release |
|------|---------|
| `410 Gone` em rotas legado | R4 |
| Remover GETs legado / model `agendamentos` | R4 |
| Remover `project_*` dual-write | R4 |
| Admin agenda writes (`/admin/agenda/*`) | dívida documentada |

## Sucessor

| Legado (removido) | CoreFlow v1 |
|-------------------|-------------|
| `POST /agenda/agendamentos` | `POST /v1/bookings` |
| `PUT/DELETE /agenda/agendamentos/{id}` | `/v1/bookings/{id}/approve\|reject\|cancel` |
| `POST /reservations` + mutations | `/v1/bookings/*` |

# Release 4 — Sprint R4-F12

**Transferência de Payment no reagendamento** (`2.15.0-r4-f12`).

| Campo | Valor |
|-------|-------|
| **Versão** | `2.15.0-r4-f12` |
| **Status** | ✅ Implemented (tech) |
| **Anterior** | R4-F11 ✅ |

## Contexto

R4-F11 copiava `deposit_paid` / `payment_status` para o booking novo, mas
as linhas `payments` e `core_payments` ficavam no `booking_id` antigo —
quebrando `GET /v1/payments?booking_id=` no substituto.

## Escopo IN

| # | Entrega |
|---|---------|
| 1 | `_transfer_payment_rows` no `RescheduleBookingHandler` |
| 2 | Reatribui `payments.booking_id` e `core_payments.booking_id` |
| 3 | Testes + docs `2.15.0-r4-f12` |

## Escopo OUT

- Duplicar/recriar Financeiro no reschedule (movimento histórico permanece)
- Transferir `queue_entries` / fila

# Release 3 — Sprint R3-F1

**Enforcement expand + production pilot** (`2.1.0-r3-f1`).

| Campo | Valor |
|-------|-------|
| **Versão** | `2.1.0-r3-f1` |
| **Status** | ✅ Implemented (tech) |
| **Flag staging/prod** | `CORE_ENFORCEMENT_MODE=block` (ou `APP_ENV=staging\|production`) |
| **Rollback** | `CORE_ENFORCEMENT_MODE=warn` (<5 min) |
| **Anterior** | R2-F6 ✅ |

## Escopo IN

| # | Entrega |
|---|---------|
| 1 | Block escritas legado **payments** → `/v1/payments` (HTTP 409) |
| 2 | Block escritas legado **fila** → `/v1/waitlist` (HTTP 409) |
| 3 | Booking legado continua bloqueado (herança F6) |
| 4 | Production resolve para `block` (piloto) |
| 5 | `/financeiro/saida` permanece warn only |
| 6 | Migration guide + release notes |

## Escopo OUT

- Remoção física de routers legado (R3-F2 / R3-F3)
- Block invoices / catalog writes
- 410 Gone (R4)

## Staging / Production

```bash
export APP_ENV=staging   # ou production
export CORE_ENFORCEMENT_MODE=block
export FEATURE_BOOKING_CORE_ENABLED=true
```

## Rollback

```bash
export CORE_ENFORCEMENT_MODE=warn
```

## Rotas bloqueadas (R3-F1)

| Legado | Successor |
|--------|-----------|
| `POST/PUT/DELETE /agenda/agendamentos`, `/agendamentos`, `/reservations` | `/v1/bookings` |
| `POST /payments/deposit`, `/payments/final`, `/pagamentos/sinal` | `/v1/payments` |
| `POST /fila` | `/v1/waitlist` |

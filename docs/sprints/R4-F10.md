# Release 4 — Sprint R4-F10

**Pagamento final core-only + sunset `/payments/deposit|final`** (`2.13.0-r4-f10`).

| Campo | Valor |
|-------|-------|
| **Versão** | `2.13.0-r4-f10` |
| **Status** | ✅ Implemented (tech) |
| **Anterior** | R4-F9 ✅ |
| **RFC** | RFC-003 · ADR-024 (sunset) · ADR-033 |

## Contexto

R4-F9 fechou o gap de `Financeiro` no deposit e sunsetou `/pagamentos/sinal*`.
O pagamento final (remaining) ainda só existia como stub legado
(`confirmar_pagamento_final` / `POST /payments/final` → NotFound/409).
Esta sprint espelha o path do deposit para o final.

## Escopo IN

| # | Entrega |
|---|---------|
| 1 | `APP_VERSION = 2.13.0-r4-f10`; tags Terraform |
| 2 | `PaymentReservationService.confirmar_pagamento_final_por_booking` (+ upsert FINAL_PAYMENT + Financeiro na 1ª confirmação) |
| 3 | `POST /admin/pagamentos/booking/{booking_id}/confirmar-final` |
| 4 | `listar_por_booking` no service |
| 5 | LegacyGone 410 em `/payments/deposit*`, `/payments/final` |
| 6 | Testes + docs |

## Escopo OUT

| Item | Release |
|------|---------|
| Reagendamento core-only (`RescheduleBookingHandler` / ADR-026) | **R4-F11** |
| Remoção física do router `payments.py` / `pagamentos.py` | Opcional |
| Migrar frontend `reservationService.reagendar` | R4-F11 |

## Testes

```
cd backend && python -m pytest tests/test_core/test_r4_f10_pagamento_final_core.py \
  tests/test_core/test_r4_f9_financeiro_pagamentos_sunset.py -q -o addopts=
```
